export const metadata = {
  title: "Sign In — BityBird Co Dashboard",
};

export default function LoginLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
