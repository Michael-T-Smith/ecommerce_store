export const metadata = {
  title: "Sign In — Lamb's Florist Dashboard",
};

export default function LoginLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
