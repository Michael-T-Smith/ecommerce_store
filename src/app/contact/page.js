import ContactPageClient from "./ContactPageClient";

export const metadata = {
  title      : "Contact & Returns — BityBird Co",
  description: "Reach out to BityBird Co with questions, order issues, or to start a return. We usually respond within one business day.",
};

export default function ContactPage() {
  return <ContactPageClient />;
}
